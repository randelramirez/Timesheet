services:

  sqlserver-api-timesheet:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      # port 5432 on localhost, and port 5432 on docker container
      - "1433:1433"
      # container_name: sql-server
    environment:
      - MSSQL_SA_PASSWORD=Password123!!!
      - ACCEPT_EULA=Y